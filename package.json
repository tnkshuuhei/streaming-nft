{
  "name": "streaming-nft",
  "version": "1.0.0",
  "author": "shutanaka",
  "description": "",
  "scripts": {
    "compile": "forge compile",
    "build": "forge fmt && forge build",
    "test": "forge test",
    "test:polygon": "forge fmt && forge test --fork-url $POLYGON_MUMBAI_RPC_URL --match-path test/Counter.t.sol",
    "deploy:mumbai": "forge fmt && forge script script/Counter.s.sol --rpc-url $POLYGON_MUMBAI_RPC_URL --broadcast -vvvv --private-key $DEPLOYER_PRIVATE_KEY --etherscan-api-key $API_KEY_POLYGONSCAN --verify",
    "test:coverage": "forge coverage",
    "format": "forge fmt",
    "clean": "rm -rf cache out"
  },
  "dependencies": {
    "ethers": "^6.13.1"
  }
}
